<div class="container">
  <div class="row">
    <% @templates.each do |template| %>
      <div class="col-md-3 col-sm-6 col-xs-12">
        <%= link_to("模板#{template.id}", new_ldpage_path, class: "btn btn-lg btn-danger") %>
      </div>
   <% end %>
    </div>
  </div>
</div>
